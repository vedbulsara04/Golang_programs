package main

import "fmt"

func main(){
	// var isValid bool = true
	// var isComplete bool = false
	
	// Boolean Operators
	fmt.Println(true && false) // Returns true only if both operands are true
	fmt.Println(true || false) // Returns true if at least one operand is true
	fmt.Println(!true) // Reverses/negates the boolean value. Think of it as the 'opposite of'
}

